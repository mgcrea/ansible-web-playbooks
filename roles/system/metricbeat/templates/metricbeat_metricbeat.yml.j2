metricbeat.modules:
- module: system
  metricsets:
    - cpu
    - filesystem
    - memory
    - network
    - process
  enabled: {{ metricbeat_module_system_enabled | bool }}
  period: 10s
  processes: ['.*']
  cpu_ticks: false

output.elasticsearch:
  hosts: {{ metricbeat_hosts | to_json }}
