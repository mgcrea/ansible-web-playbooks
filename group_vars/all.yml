---
# File: all.yml
# Type: group_vars

# Use this file to override default configuration
# that lives in `defaults` folders

# databases/beanstalkd
beanstalkd_path: /srv/data/beanstalkd

# databases/mongodb
mongodb_path: /srv/data/mongodb

# databases/mysql
mysql_path: /srv/data/mysql

# databases/redis
redis_path: /srv/data/redis
